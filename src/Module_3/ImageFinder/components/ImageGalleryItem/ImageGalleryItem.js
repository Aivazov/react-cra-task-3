export const ImageGalleryItem = (images) => {
  console.log('images in the gallery items', images.images);
  // console.log('item', images[0]);
  // const imagesMapped = images.map((item) => item);
  // console.log('imagesMapped', imagesMapped);
  return (
    <li className="gallery-item">
      Here will be images
      <img src="" alt="" />
    </li>
  );
};
